"use strict";
(() => {
    //Tuple
    //Típicamente un arreglo contiene de cero a muchos objetos de un solo tipo. 
    const hero = ['Dr Strange', 100, true];
    hero[0] = 'Ironman';
    hero[1] = 50;
    hero[2] = false;
    console.log(hero);
})();
